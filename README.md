
  # Uri Website V.4 (Copy)

  This is a code bundle for Uri Website V.4 (Copy). The original project is available at https://www.figma.com/design/BZR6Pa40rPocRTynCGet24/Uri-Website-V.4--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  